# Эмуляция биллинговой системы

Pet-проект — модульная система для эмуляции биллинговых процессов с REST API.
Основная цель: реализация механизма тарификации абонентов с использованием многопоточности и ручного управления транзакциями.

## 🧰 Технологии
- **Backend:** Java 21, Spring Boot (Web, Data JPA, Validation)
- **База данных:** PostgreSQL
- **Документация:** Swagger/OpenAPI
- **Контейнеризация:** Docker, Docker Compose
- **Сборка:** Maven (Layered Jar build)

## 📐 Архитектура

```mermaid
Soon...
```

### Ключевые архитектурные решения

**1. Процесс Биллинга (`BillingCycleService`)**
Самая нагруженная часть системы, реализованная с учетом масштабируемости:
- **Пагинация:** Абоненты обрабатываются страницами (batch processing) для экономии памяти.
- **Многопоточность:** Обработка страниц распараллеливается через `ThreadPoolTaskExecutor`.
- **Транзакции:** Использование `TransactionTemplate` для ручного управления транзакциями внутри потоков гарантирует атомарность списания средств.

**2. Отчетность (`ReportRepository`)**
Для аналитики (агрегация, суммы) используется **Native SQL** через `JdbcTemplate` для максимальной производительности, исключая оверхед JPA.

**3. Docker Optimization**
В `Dockerfile` используется `Layertools` для кэширования слоев зависимостей, что ускоряет повторную сборку.

## 🚀 Запуск

```bash
# 1. Клонировать репозиторий
git clone https://github.com/nv/billing.git
cd billing

# 2. Запустить через Docker (автоматическая сборка)
docker-compose up -d

# 3. Проверить работу (Swagger UI)
# Откройте в браузере: http://localhost:8080/swagger-ui/index.html
```

> **Альтернативно:** Можно использовать скрипт `./deploy.sh` для полного цикла пересборки и запуска.

## 📖 API

Основные endpoints:
- `POST /api/subscribers` — Регистрация абонента
- `POST /api/tariffs` — Создание тарифа
- `POST /api/admin/run-billing-cycle` — Запуск процесса биллинга (асинхронно)
- `GET /api/reports/tariffs` — Статистика по тарифам (доход, кол-во абонентов)

Полная документация доступна после запуска:
- Swagger UI: `http://localhost:8080/swagger-ui/index.html`
- OpenAPI спецификация: `http://localhost:8080/v3/api-docs`

## 💡 Пример сценария

1. Создать клиента (абонента) через API
2. Назначить тарифный план
3. Загрузить данные об использовании (опционально)
4. Запустить расчетный цикл (`run-billing-cycle`)
5. Получить сгенерированный отчет

## 🧪 Тестирование

```bash
# Запустить тесты
./mvnw test
```

## 📁 Структура проекта

```
Soon...
```

## Планы по доработке (To-Do)
* [x] Добавить Swagger (OpenAPI) для документации.
* [x] Реализовать механизм Retry для упавших транзакций.
* [ ] Вынести биллинг-цикл в отдельный микросервис (Scheduled Job).
* [x] Покрыть тестами `BillingCycleService` (Testcontainers).
